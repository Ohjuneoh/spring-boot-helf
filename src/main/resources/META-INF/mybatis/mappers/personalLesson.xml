<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.co.helf.mapper.PersonalLessonMapper">
	<select id="getAllTrainers" resultType="User">
		select
			b.trainer_no,
			a.user_name,
			b.trainer_file
		from
			helf_users a, helf_trainers b
		where
			a.user_id = b.user_id
		order by
			b.trainer_no
	</select>
	<select id="getCareerByTrainerNo" parameterType="int" resultType="Career">
		select
			career_no,
			career_name,
			career_start_date,
			career_end_date,
			trainer_no
		from helf_trainer_career
		where
			trainer_no = #{value}
		order by
			career_start_date;
	</select>

</mapper>
